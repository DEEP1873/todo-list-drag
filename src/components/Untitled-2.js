           {/* {row.map((col, colindex) => {
              const isDark = (rowIndex + colindex) % 2 === 1;
              return (
                <div
                  key={colindex}
                  className={`flex items-center text-3xl h-14 w-14 justify-center  ${
                    isDark ? "bg-[#b58863]" : "bg-[#f0d9b5]"
                  }`}
                >{col && <span>{pieces[col]}</span>}</div>
              );
            })} */}




        // if (newboard[row][col] != "") {
        //   // console.log(newboard[row+1][col]);
        //   console.log("we can't move here");
        //   return;
        // }




  //         const pawn = (row: number, col: number) => {
  //   if(select){
  //     const pawnarr = board.map((prev) => [...prev]);
  //     pawnarr[row][col] = board[select[0]][select[1]];
  //     pawnarr[select[0]][select[1]] ="";
  //     setBoard(pawnarr);
  //     setSelect(null);
      
  //   }

  // };

  